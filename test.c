#include <stdio.h>
int main(){
/*for(int i = 0; i <= 100; i += 10) {
    printf("\rLoading: [%d%%]", i);
    fflush(stdout); // Forces the text to show up immediately
    sleep(1);       // Wait a second
}
*/

printf("\x7 \n \xb \n \013 \n \007");
return 0;
}
